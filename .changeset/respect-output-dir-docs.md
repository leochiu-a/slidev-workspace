---
"slidev-workspace": patch
---

Make the build step copy slide bundles into whatever `outputDir` is configured (default `./dist`) and expand the docs with the new option plus GitHub Pages/Vercel deployment guidance.
